import { ReactComponent as Arrow } from "./arrow.svg";
import CreateIcon from "../utils/CreateIcon";

const IconArrow = (props) => (<CreateIcon {...props}><Arrow /></CreateIcon>);

export default IconArrow;
